{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "clang-format",
            "type": "shell",
            "command": "find . -iname *.h -o -iname *.c | xargs clang-format -i --verbose",
            "presentation": {
                "reveal": "never"
            },
            "problemMatcher": []
        }
    ]
}